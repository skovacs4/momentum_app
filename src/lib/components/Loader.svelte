<script>
    import Loader from 'react-loader-spinner';
  
    export let type = 'ThreeDots'; // Default type of loader
    export let color = '#6a5acd'; // Default color of loader
    export let height = 100; // Default height of loader
    export let width = 100; // Default width of loader
  </script>
  
  <Loader
    type={type}
    color={color}
    height={height}
    width={width}
  />
  